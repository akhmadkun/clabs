Objective: Secure, Optimized, and Leaked IS-IS Routing
Task 1 (Wide Metrics & IPv6 Baseline):

    + Configure IS-IS for both IPv4 and IPv6
    + Change the standard IS-IS metric style on all routers to Wide Metrics

Task 2 (Multi-Topology IPv6):

    + By default, IS-IS shares the same topology tree for IPv4 and IPv6.
    + Separate the SPF calculations by enabling the IPv6 Unicast Topology

Task 3 (Security - LSP Authentication):

    + Secure the control plane.
    + Enable HMAC-MD5 authentication for LSP exchanges between R2 and R3.
    + Use password KunciRahasia.

Task 4 (The Boss Level: Route Leaking):

    + By default, R1 (Level 1) only has a default route to reach R4.
    + On R2, leak the specific Loopback routes of R4 to Level 1 database.
    + Leak Both of it's IPv4 and IPv6 address.
    + R1 must have specific routes to R4's Loopback (IPv4 and IPv6)
